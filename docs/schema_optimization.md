# Schema Optimization

Schema optimization rewrites description text only. It does not change tool
slugs, parameter names, required fields, parameter types, auth behavior,
execution behavior, or return schemas.

The deterministic validator rejects structural drift and removed safety terms.

