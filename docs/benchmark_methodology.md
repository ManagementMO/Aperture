# Benchmark Methodology

The deterministic benchmark compares the same fixture tasks across modes:

- `raw`
- `aperture_compressed`
- `aperture_cached`
- `aperture_full`
- `shadow`

Metrics include raw tokens, compressed tokens, tokens saved, compression ratio,
cache hits, API calls avoided, schema savings, task success, missing critical
fields, and latency.

